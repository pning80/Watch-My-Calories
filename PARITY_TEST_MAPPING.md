# Parity Audit — Test → Ledger Mapping

Auto-generated by `/tmp/parity-audit/map_tests_to_ledger.py`. For each new XCUITest function added in PR #1 (under `// MARK: - Parity audit (2026-05-30)` sections), this table reports the `PARITY_LEDGER.md` row it covers + the confidence of the match.

**Heuristic:** test-name keyword overlap with ledger row text is the primary signal, plus assertion-context (only IDs/labels inside `XCTAssert*` blocks, not setup-helper calls) and gesture compatibility.

**Summary:** 61 mapped · 0 unmatched · HIGH=60 · MEDIUM=1 · LOW=0

## AppMenuTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testAboutHelpAndSupportLinkTaps` | AboutView #3 — Help & Support | 18 | name tokens match: ['help', 'support']; asserts on a11y_id `about_helpAndSupport` |
| **HIGH** | `testAboutPrivacyPolicyLinkTaps` | AboutView #4 — Privacy Policy | 18 | name tokens match: ['privacy', 'policy']; asserts on a11y_id `about_privacyPolicy` |
| **HIGH** | `testAboutRateOnAppStoreReachable` | AboutView #2 — Rate on App Store | 23 | name tokens match: ['rate', 'app', 'store']; asserts on a11y_id `about_rateOnAppStore` |
| **HIGH** | `testAboutVersionLabelExists` | AboutView #1 — Version label (copy) | 13 | name tokens match: ['version']; asserts on a11y_id `about_versionLabel` |
| **HIGH** | `testAboutVersionTapCopiesToClipboard` | AboutView #1 — Version label (copy) | 23 | name tokens match: ['version', 'clipboard']; asserts on a11y_id `about_versionLabel`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testSettingsBannerAdHiddenUnderUITesting` | AppMenu / Toolbar #2 — Settings menu item | 22 | preferred screen (AppMenu / Toolbar); name tokens match: ['settings']; assertion labels mention: ['Settings']; gesture compatible (['tap'] ↔ 'tap') |

## DashboardTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testEditEntryFromDashboardContextMenu` | DashboardView #10 — Food Entry Group Card – context menu (multi-item) | 31 | preferred screen (DashboardView); name tokens match: ['edit', 'entry', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **HIGH** | `testEditMealGroupFromDashboardContextMenu` | DashboardView #10 — Food Entry Group Card – context menu (multi-item) | 31 | preferred screen (DashboardView); name tokens match: ['edit', 'group', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **HIGH** | `testEditSubItemFromDashboardContextMenu` | DashboardView #12 — Food Entry Group Card – sub-item row context menu | 36 | preferred screen (DashboardView); name tokens match: ['edit', 'sub', 'item', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **HIGH** | `testMultiItemMealGroupCardShowsMealName` | Components / SharedComponents #22 — EditMealGroupView – Item Name TextField | 25 | name tokens match: ['item', 'meal', 'group', 'meal', 'name'] |
| **HIGH** | `testMultiItemMealGroupLongPressShowsContextMenu` | DashboardView #10 — Food Entry Group Card – context menu (multi-item) | 36 | preferred screen (DashboardView); name tokens match: ['multi', 'item', 'group', 'context', 'menu']; gesture compatible (['long-press'] ↔ 'long-press') |
| **HIGH** | `testMultiItemMealGroupSummaryRowExpandsToShowItems` | DashboardView #8 — Food Entry Group Card – summary row | 21 | preferred screen (DashboardView); name tokens match: ['group', 'summary']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testMultiItemMealSubItemLongPressShowsContextMenu` | DashboardView #10 — Food Entry Group Card – context menu (multi-item) | 36 | preferred screen (DashboardView); name tokens match: ['multi', 'item', 'item', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **HIGH** | `testThumbnailTapOpensFullScreenImage` | DashboardView #9 — Food Entry Group Card – thumbnail | 31 | preferred screen (DashboardView); name tokens match: ['thumbnail', 'full', 'screen', 'image']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testViewEntryFromDashboardContextMenu` | DashboardView #10 — Food Entry Group Card – context menu (multi-item) | 26 | preferred screen (DashboardView); name tokens match: ['entry', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **MEDIUM** | `testPullToRefreshGestureCompletesWithoutError` | DashboardView #13 — Pull-to-refresh | 12 | preferred screen (DashboardView); name tokens match: ['pull', 'refresh']; gesture mismatch penalty (['long-press'] ↔ 'pull-down') |

## EstimationReviewTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testAIConsentDeclineDismissesEstimation` | EstimationReviewView #10 — AI Consent – Decline | 21 | preferred screen (EstimationReviewView); name tokens match: ['consent', 'decline']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testAIConsentSheetFiresWhenConsentMissing` | EstimationReviewView #9 — AI Consent – Accept | 21 | preferred screen (EstimationReviewView); name tokens match: ['consent', 'consent']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testErrorStateShowDetailsButton` | EstimationReviewView #2 — Show/Hide Details (error) | 21 | preferred screen (EstimationReviewView); name tokens match: ['error', 'show', 'details'] |
| **HIGH** | `testErrorStateShowsCancelButton` | EstimationReviewView #4 — Cancel (error) | 22 | preferred screen (EstimationReviewView); name tokens match: ['error', 'cancel']; assertion labels mention: ['Cancel', 'Cancel button must appear on error state'] |
| **HIGH** | `testErrorStateShowsTryAgainButton` | EstimationReviewView #3 — Try Again (error) | 21 | preferred screen (EstimationReviewView); name tokens match: ['error', 'try', 'again'] |
| **HIGH** | `testErrorStateTryAgainTapResetsView` | EstimationReviewView #3 — Try Again (error) | 29 | preferred screen (EstimationReviewView); name tokens match: ['error', 'try', 'again']; assertion labels mention: ['Try Again']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testNoFoodStateShowsCancelButton` | EstimationReviewView #7 — Cancel (no-food) | 19 | preferred screen (EstimationReviewView); name tokens match: ['food', 'cancel']; assertion labels mention: ['Cancel'] |
| **HIGH** | `testNoFoodStateShowsTryAgainButton` | EstimationReviewView #6 — Try Again (no-food) | 24 | preferred screen (EstimationReviewView); name tokens match: ['food', 'try', 'again']; assertion labels mention: ['Try Again'] |

## HistoryTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testHistoryMultiItemMealGroupAppears` | Components / SharedComponents #22 — EditMealGroupView – Item Name TextField | 20 | name tokens match: ['item', 'meal', 'group']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testHistoryMultiItemMealGroupExpandsItems` | HistoryView #4 — History Day Card – header | 24 | preferred screen (HistoryView); name tokens match: ['history']; asserts on a11y_id `history_dayCard`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testHistoryMultiItemMealGroupLongPressShowsContextMenu` | HistoryView #8 — Food Entry Group Card – context menu (multi-item) | 36 | preferred screen (HistoryView); name tokens match: ['multi', 'item', 'group', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **HIGH** | `testHistorySubItemLongPressShowsContextMenu` | HistoryView #10 — Food Entry Group Card – sub-item row context menu | 31 | preferred screen (HistoryView); name tokens match: ['sub', 'item', 'context', 'menu']; gesture compatible (['long-press', 'tap'] ↔ 'long-press') |
| **HIGH** | `testHistoryThumbnailTapOpensFullScreenImage` | HistoryView #7 — Food Entry Group Card – thumbnail | 31 | preferred screen (HistoryView); name tokens match: ['thumbnail', 'full', 'screen', 'image']; gesture compatible (['tap'] ↔ 'tap') |

## ManualEntryTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testCarbsFieldAcceptsInput` | ManualEntryView #6 — Carbs TextField (optional) | 16 | preferred screen (ManualEntryView); name tokens match: ['carbs']; gesture compatible (['tap', 'type-text'] ↔ 'tap') |
| **HIGH** | `testFatFieldAcceptsInput` | ManualEntryView #7 — Fat TextField (optional) | 16 | preferred screen (ManualEntryView); name tokens match: ['fat']; gesture compatible (['tap', 'type-text'] ↔ 'tap') |
| **HIGH** | `testManualEntryWithFullNutritionSaves` | ManualEntryView #10 — Save (toolbar) | 19 | preferred screen (ManualEntryView); asserts on a11y_id `manualEntry_saveButton`; gesture compatible (['tap', 'type-text'] ↔ 'tap') |
| **HIGH** | `testProteinFieldAcceptsInput` | ManualEntryView #5 — Protein TextField (optional) | 16 | preferred screen (ManualEntryView); name tokens match: ['protein']; gesture compatible (['tap', 'type-text'] ↔ 'tap') |

## OnboardingTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testGetStartedButtonAdvancesFromWelcome` | OnboardingView #2 — Get Started button (step 0) | 29 | preferred screen (OnboardingView); name tokens match: ['get', 'started']; asserts on a11y_id `onboarding_getStartedButton`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testGoalStepActivityPickerCanChangeSelection` | OnboardingView #10 — Activity Level picker | 21 | preferred screen (OnboardingView); name tokens match: ['activity', 'picker']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testGoalStepAgePickerExists` | OnboardingView #8 — Age picker | 16 | preferred screen (OnboardingView); name tokens match: ['age', 'picker'] |
| **HIGH** | `testGoalStepGenderPickerCanChangeSelection` | OnboardingView #9 — Gender picker | 21 | preferred screen (OnboardingView); name tokens match: ['gender', 'picker']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testGoalStepHeightPickerExists` | OnboardingView #3 — Height (US) Feet picker | 19 | preferred screen (OnboardingView); name tokens match: ['height', 'picker']; assertion labels mention: ['Height'] |
| **HIGH** | `testGoalStepWeightPickerExists` | OnboardingView #5 — Weight (US) picker | 22 | preferred screen (OnboardingView); name tokens match: ['weight', 'picker']; assertion labels mention: ['Weight'] |
| **HIGH** | `testNextButtonAdvancesFromPermissions` | OnboardingView #15 — Next button (step 1) | 24 | preferred screen (OnboardingView); name tokens match: ['next']; asserts on a11y_id `onboarding_nextButton`; gesture compatible (['tap'] ↔ 'tap') |

## ScanMenuTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testMenuCameraCancelDismissesToDashboard` | ScanMenuSheet #1 — Scan Menu | 30 | preferred screen (ScanMenuSheet); name tokens match: ['menu']; asserts on a11y_id `scanMenuSheet_scan`; assertion labels mention: ['Scan Menu']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testMenuCameraScreenHasCaptureButton` | ScanMenuSheet #1 — Scan Menu | 30 | preferred screen (ScanMenuSheet); name tokens match: ['menu']; asserts on a11y_id `scanMenuSheet_scan`; assertion labels mention: ['Scan Menu']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testMenuScanDetailDeleteButtonShowsConfirmation` | ScannedMenusView #6 — Menu Scan Detail – Delete (toolbar) | 31 | name tokens match: ['menu', 'scan', 'detail', 'delete']; assertion labels mention: ['Delete', 'Scan Menu']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testScanMenuSheetChooseFromLibraryButtonOpensPicker` | ScanMenuSheet #2 — Choose from Library | 29 | preferred screen (ScanMenuSheet); name tokens match: ['choose', 'library']; asserts on a11y_id `scanMenuSheet_chooseFromLibrary`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testScanMenuSheetScanButtonOpensCamera` | ScanMenuSheet #1 — Scan Menu | 40 | preferred screen (ScanMenuSheet); name tokens match: ['scan', 'menu', 'scan']; asserts on a11y_id `scanMenuSheet_scan`; assertion labels mention: ['Scan Menu']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testScannedMenusEditButtonTogglesMode` | ScanMenuSheet #3 — Stored Menus | 24 | preferred screen (ScanMenuSheet); name tokens match: ['menus']; asserts on a11y_id `scanMenuSheet_storedMenus`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testScannedMenusListShowsRowsWithSeedData` | ScanMenuSheet #3 — Stored Menus | 24 | preferred screen (ScanMenuSheet); name tokens match: ['menus']; asserts on a11y_id `scanMenuSheet_storedMenus`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testScannedMenusSwipeToDeleteRevealsAction` | ScanMenuSheet #3 — Stored Menus | 24 | preferred screen (ScanMenuSheet); name tokens match: ['menus']; asserts on a11y_id `scanMenuSheet_storedMenus`; gesture compatible (['swipe-left', 'tap'] ↔ 'tap') |
| **HIGH** | `testScannedMenusTappingRowOpensDetail` | ScanMenuSheet #3 — Stored Menus | 24 | preferred screen (ScanMenuSheet); name tokens match: ['menus']; asserts on a11y_id `scanMenuSheet_storedMenus`; gesture compatible (['tap'] ↔ 'tap') |

## SettingsTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testActivityPickerCanChangeSelection` | SettingsView #14 — Activity Level picker | 29 | preferred screen (SettingsView); name tokens match: ['activity', 'picker']; asserts on a11y_id `settings_activityPicker`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testAgeDisclosureGroupExpands` | SettingsView #11 — Age DisclosureGroup | 26 | preferred screen (SettingsView); name tokens match: ['age', 'disclosure', 'group']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testGenderPickerCanChangeSelection` | SettingsView #13 — Gender picker | 29 | preferred screen (SettingsView); name tokens match: ['gender', 'picker']; asserts on a11y_id `settings_genderPicker`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testHeightDisclosureGroupExpandsInMetricMode` | SettingsView #7 — Height (Metric) DisclosureGroup | 37 | preferred screen (SettingsView); name tokens match: ['height', 'disclosure', 'group', 'metric']; assertion labels mention: ['Height']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testHeightFeetAndInchesPickersExistInUSMode` | SettingsView #3 — Height (US) – Feet picker | 21 | preferred screen (SettingsView); name tokens match: ['height', 'feet']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testKeyboardDoneButtonDismissesKeyboard` | SettingsView #21 — Done button (keyboard) | 29 | preferred screen (SettingsView); name tokens match: ['keyboard', 'done', 'keyboard']; assertion labels mention: ['Done']; gesture compatible (['swipe-up', 'tap'] ↔ 'tap') |
| **HIGH** | `testManagePrivacyChoicesButtonHiddenUnderUITesting` | SettingsView #18 — Manage Privacy Choices button | 23 | preferred screen (SettingsView); name tokens match: ['manage', 'privacy', 'choices']; assertion labels mention: ['Privacy Choices']; gesture mismatch penalty (['swipe-up'] ↔ 'tap') |
| **HIGH** | `testThemePickerCanChangeSelection` | SettingsView #1 — Theme picker | 34 | preferred screen (SettingsView); name tokens match: ['theme', 'picker', 'change']; asserts on a11y_id `settings_themePicker`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testUnitPickerCanChangeSelection` | SettingsView #2 — Unit System picker | 34 | preferred screen (SettingsView); name tokens match: ['unit', 'picker', 'change']; asserts on a11y_id `settings_unitPicker`; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testWeightDisclosureGroupExpandsInMetricMode` | SettingsView #9 — Weight (Metric) DisclosureGroup | 37 | preferred screen (SettingsView); name tokens match: ['weight', 'disclosure', 'group', 'metric']; assertion labels mention: ['Weight']; gesture compatible (['tap'] ↔ 'tap') |

## TabNavigationTests.swift

| Confidence | Test function | Ledger row | Score | Why |
|---|---|---|---:|---|
| **HIGH** | `testCameraRootCancelReturnsToDashboard` | ContentView #5 — Camera root – Cancel | 32 | preferred screen (ContentView); name tokens match: ['camera', 'root', 'cancel']; assertion labels mention: ['Cancel']; gesture compatible (['tap'] ↔ 'tap') |
| **HIGH** | `testScannedMenusDoneReturnsToDashboard` | ContentView #7 — Scanned Menus – Done | 32 | preferred screen (ContentView); name tokens match: ['scanned', 'menus', 'done']; assertion labels mention: ['Done']; gesture compatible (['tap'] ↔ 'tap') |

