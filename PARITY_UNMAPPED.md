# Parity Audit — Unmapped Tests + Uncovered Rows

## Tests that didn't match any ledger row

(none)

## Ledger rows still uncovered by PR #1's NEW tests

(Excludes display-only rows. Some may be covered by EXISTING baseline tests that PR #1 didn't add — this list is just 'not closed by PR #1's new tests'.)

- DashboardView #1 — App Menu Button (`appMenu_button` / tap)
- DashboardView #2 — Empty State Card (`dashboard_emptyStateCard` / tap)
- DashboardView #3 — Manual Entry Link (`dashboard_manualEntryLink` / tap)
- DashboardView #11 — Food Entry Group Card – context menu (single item) (`(no id)` / long-press)
- DashboardView #14 — Full-screen image cover (after thumbnail tap) (`(no id)` / tap)
- HistoryView #2 — App Menu Button (`appMenu_button` / tap)
- HistoryView #3 — Empty state card (`history_emptyState` / tap)
- HistoryView #6 — Food Entry Group Card – summary row (`(no id)` / tap)
- HistoryView #9 — Food Entry Group Card – context menu (single item) (`(no id)` / long-press)
- HistoryView #11 — Full-screen image cover (`(no id)` / tap)
- SettingsView #4 — Height (US) – Inches picker (`(no id)` / tap)
- SettingsView #5 — Weight (US) DisclosureGroup (`(no id)` / tap)
- SettingsView #6 — Weight (US) wheel (`(no id)` / tap/swipe)
- SettingsView #8 — Height (Metric) wheel (`(no id)` / tap/swipe)
- SettingsView #10 — Weight (Metric) wheel (`(no id)` / tap/swipe)
- SettingsView #12 — Age wheel (`(no id)` / tap/swipe)
- SettingsView #15 — Target Calories TextField (`settings_targetCalories` / tap)
- SettingsView #16 — Calculate Recommended Goal button (`settings_calculateGoal` / tap)
- SettingsView #17 — AI Photo Analysis toggle (`settings_aiConsentToggle` / tap)
- SettingsView #19 — Cancel button (toolbar) (`(no id)` / tap)
- SettingsView #20 — Save button (toolbar) (`settings_saveButton` / tap)
- SettingsView #22 — Discard alert – Discard Changes (`(no id)` / tap)
- SettingsView #23 — Discard alert – Keep Editing (`(no id)` / tap)
- OnboardingView #1 — Skip button (top-right) (`onboarding_skipButton` / tap)
- OnboardingView #4 — Height (US) Inches picker (`(no id)` / tap)
- OnboardingView #6 — Height (Metric) picker (`(no id)` / tap/swipe)
- OnboardingView #7 — Weight (Metric) picker (`(no id)` / tap/swipe)
- OnboardingView #11 — Target Calories TextField (`onboarding_targetCalories` / tap)
- OnboardingView #12 — Calculate Recommended Goal button (`onboarding_calculateGoal` / tap)
- OnboardingView #13 — AI Photo Analysis toggle (step 1) (`onboarding_aiConsentToggle` / tap)
- OnboardingView #14 — Connect Apple Health button (`onboarding_connectHealth` / tap)
- OnboardingView #16 — Start Tracking button (step 2) (`onboarding_finishButton` / tap)
- OnboardingView #17 — Keyboard Done button (`(no id)` / tap)
- CameraView #2 — Capture button (`camera_captureButton` / tap)
- CameraView #3 — MealTypePicker – Breakfast (`mealTypePicker` / tap)
- CameraView #4 — MealTypePicker – Lunch (`mealTypePicker` / tap)
- CameraView #5 — MealTypePicker – Dinner (`mealTypePicker` / tap)
- CameraView #6 — MealTypePicker – Snack (`mealTypePicker` / tap)
- CameraView #7 — Retake button (`camera_retakeButton` / tap)
- CameraView #8 — Use Photo button (`camera_usePhotoButton` / tap)
- CameraView #9 — Open Settings button (cam denied) (`(no id)` / tap)
- CameraView #10 — Calorie Disclaimer – Continue (`(no id)` / tap)
- CameraView #11 — Calorie Disclaimer – Don't Show toggle (`disclaimer_dontShowToggle` / tap)
- CameraView #12 — Camera Alert – OK (`(no id)` / tap)
- MenuCameraView #2 — Capture button (`(no id)` / tap)
- MenuCameraView #3 — Retake button (`(no id)` / tap)
- MenuCameraView #4 — Analyze Menu button (`(no id)` / tap)
- MenuCameraView #5 — Open Settings button (cam denied) (`(no id)` / tap)
- MenuCameraView #6 — Calorie Disclaimer – Continue (`(no id)` / tap)
- MenuCameraView #7 — Calorie Disclaimer – Don't Show toggle (`disclaimer_dontShowToggle` / tap)
- MenuCameraView #8 — Camera Alert – OK (`(no id)` / tap)
- MenuCameraView #9 — Menu Photo Library picker (PhotosUI) (`(no id)` / tap)
- MenuCameraView #10 — Menu Photo – Reselect (`(no id)` / tap)
- MenuCameraView #11 — Menu Photo – Use (`(no id)` / tap)
- EstimationReviewView #1 — Native ad (`ads_native` / tap)
- EstimationReviewView #5 — View Results (complete) (`ads_viewResultsButton` / tap)
- EstimationReviewView #8 — Done (success) (`review_doneButton` / tap)
- MenuAnalysisView #1 — Native ad (`ads_native` / tap)
- MenuAnalysisView #2 — Show Details (error) (`(no id)` / tap)
- MenuAnalysisView #3 — Try Again (error) (`(no id)` / tap)
- MenuAnalysisView #4 — Cancel (error) (`(no id)` / tap)
- MenuAnalysisView #5 — Scan Again (success) (`(no id)` / tap)
- MenuAnalysisView #6 — Done (success) (`(no id)` / tap)
- MenuAnalysisView #7 — Try Again (not-a-menu) (`(no id)` / tap)
- MenuAnalysisView #8 — AI Consent – Accept (`(no id)` / tap)
- MenuAnalysisView #9 — AI Consent – Decline (`(no id)` / tap)
- ScannedMenusView #2 — Edit button (toolbar) (`(no id)` / tap)
- ScannedMenusView #3 — Scanned menu row (`(no id)` / tap)
- ScannedMenusView #4 — Scanned menu row – swipe delete (`(no id)` / swipe-left)
- ScannedMenusView #5 — Menu Scan Detail – photo (`(no id)` / tap)
- ScannedMenusView #7 — Menu Scan Detail – Delete confirm (`(no id)` / tap)
- PhotoLibraryReviewView #1 — PhotosPicker (`(no id)` / tap)
- PhotoLibraryReviewView #2 — MealTypePicker – Breakfast (`mealTypePicker` / tap)
- PhotoLibraryReviewView #3 — MealTypePicker – Lunch (`mealTypePicker` / tap)
- PhotoLibraryReviewView #4 — MealTypePicker – Dinner (`mealTypePicker` / tap)
- PhotoLibraryReviewView #5 — MealTypePicker – Snack (`mealTypePicker` / tap)
- PhotoLibraryReviewView #6 — Reselect (`photoLibrary_chooseAgainButton` / tap)
- PhotoLibraryReviewView #7 — Use Photo (`photoLibrary_usePhotoButton` / tap)
- PhotoLibraryReviewView #8 — Calorie Disclaimer – Continue (`disclaimer_continueButton` / tap)
- PhotoLibraryReviewView #9 — Calorie Disclaimer – Don't Show toggle (`disclaimer_dontShowToggle` / tap)
- LogFoodSheet #1 — Scan Food (`(no id)` / tap)
- LogFoodSheet #2 — Choose from Library (`(no id)` / tap)
- LogFoodSheet #3 — Log Manually (`(no id)` / tap)
- ContentView #1 — Dashboard tab (`tab_dashboard` / tap)
- ContentView #2 — Log Food tab (intercepted) (`(no id)` / tap)
- ContentView #3 — Scan Menu tab (intercepted) (`(no id)` / tap)
- ContentView #4 — History tab (`tab_history` / tap)
- ContentView #6 — Menu Camera root – Cancel (`(no id)` / tap)
- ManualEntryView #1 — Food Name TextField (`manualEntry_foodName` / tap)
- ManualEntryView #2 — Calories TextField (`manualEntry_calories` / tap)
- ManualEntryView #3 — Quantity TextField (`manualEntry_quantity` / tap)
- ManualEntryView #4 — Meal Type picker (`manualEntry_mealPicker` / tap)
- ManualEntryView #8 — Nutrition DisclosureGroup (`(no id)` / tap)
- ManualEntryView #9 — Cancel (toolbar) (`manualEntry_cancelButton` / tap)
- ManualEntryView #11 — Done (keyboard) (`(no id)` / tap)
- Components / SharedComponents #1 — FullScreenImageView – pinch (`(no id)` / pinch)
- Components / SharedComponents #2 — FullScreenImageView – drag (`(no id)` / drag)
- Components / SharedComponents #3 — FullScreenImageView – double-tap (`(no id)` / double-tap)
- Components / SharedComponents #4 — FullScreenImageView – Close (`(no id)` / tap)
- Components / SharedComponents #5 — FoodEntryCard – thumbnail (`(no id)` / tap)
- Components / SharedComponents #6 — FoodEntryCard – context menu (`(no id)` / long-press)
- Components / SharedComponents #7 — FoodEntryGroupCard – summary row (`(no id)` / tap)
- Components / SharedComponents #8 — FoodEntryGroupCard – thumbnail (`(no id)` / tap)
- Components / SharedComponents #9 — FoodEntryGroupCard – ctx menu (group) (`(no id)` / long-press)
- Components / SharedComponents #10 — FoodEntryGroupCard – ctx menu (item) (`(no id)` / long-press)
- Components / SharedComponents #11 — FoodEntryGroupCard – sub-item ctx menu (`(no id)` / long-press)
- Components / SharedComponents #12 — EditFoodEntryView – Food Name TextField (`(no id)` / tap)
- Components / SharedComponents #13 — EditFoodEntryView – Meal Name TextField (`(no id)` / tap)
- Components / SharedComponents #14 — EditFoodEntryView – Calories TextField (`(no id)` / tap)
- Components / SharedComponents #15 — EditFoodEntryView – Quantity TextField (`(no id)` / tap)
- Components / SharedComponents #16 — EditFoodEntryView – Meal Type Picker (`(no id)` / tap)
- Components / SharedComponents #17 — EditFoodEntryView – Nutrition DisclosureGroup (`(no id)` / tap)
- Components / SharedComponents #18 — EditFoodEntryView – Cancel (`(no id)` / tap)
- Components / SharedComponents #19 — EditFoodEntryView – Save (`(no id)` / tap)
- Components / SharedComponents #20 — EditMealGroupView – Meal Name TextField (`(no id)` / tap)
- Components / SharedComponents #21 — EditMealGroupView – Meal Type Picker (`(no id)` / tap)
- Components / SharedComponents #23 — EditMealGroupView – Item Calories TextField (`(no id)` / tap)
- Components / SharedComponents #24 — EditMealGroupView – Item Quantity TextField (`(no id)` / tap)
- Components / SharedComponents #25 — EditMealGroupView – Item Nutrition DisclosureGroup (`(no id)` / tap)
- Components / SharedComponents #26 — EditMealGroupView – Cancel (`(no id)` / tap)
- Components / SharedComponents #27 — EditMealGroupView – Save (`(no id)` / tap)
- Components / SharedComponents #28 — ViewFoodEntryView – Done (`(no id)` / tap)
- Components / SharedComponents #29 — ViewMealGroupView – Done (`(no id)` / tap)
- Components / SharedComponents #30 — AIConsentSheet – Allow (`(no id)` / tap)
- Components / SharedComponents #31 — AIConsentSheet – Don't Allow (`(no id)` / tap)
- AppMenu / Toolbar #1 — Menu button (`appMenu_button` / tap)
- AppMenu / Toolbar #3 — About menu item (`(no id)` / tap)
- AppMenu / Toolbar #4 — About sheet – Done (`(no id)` / tap)
- BannerAdView / NativeAdView #1 — Banner Ad (`ads_banner` / tap)
- BannerAdView / NativeAdView #2 — Native Ad container (`ads_native` / tap)
- BannerAdView / NativeAdView #3 — Native Ad – CTA button (`(no id)` / tap)
